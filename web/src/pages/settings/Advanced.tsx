// Placeholder pane — real implementation lands in Phase 11.5.

export function SettingsAdvanced() {
  return (
    <p className="text-sm text-muted-foreground">
      Coming in Phase 11.5 — Monaco YAML editor with live JSON-schema
      validation and diff preview before save.
    </p>
  );
}
