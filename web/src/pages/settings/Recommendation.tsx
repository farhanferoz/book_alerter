// Placeholder pane — real implementation lands in Phase 11.3.

export function SettingsRecommendation() {
  return (
    <p className="text-sm text-muted-foreground">
      Coming in Phase 11.3 — sliders for min_obs, buy_pct, watch_pct,
      target_tolerance_pct, and dedup_window_hours.
    </p>
  );
}
