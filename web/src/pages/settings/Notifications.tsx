// Placeholder pane — real implementation lands in Phase 11.4.

export function SettingsNotifications() {
  return (
    <p className="text-sm text-muted-foreground">
      Coming in Phase 11.4 — per-channel config, &ldquo;Send test&rdquo;,
      per-kind toggles, and quiet hours.
    </p>
  );
}
